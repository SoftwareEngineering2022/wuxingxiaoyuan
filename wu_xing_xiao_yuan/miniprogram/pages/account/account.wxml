<!-- 接单数量、余额、头像、个人信息 -->
<view class="title">

  <view wx:if="{{avatarUrl!=''}}">
    <view class="img">
      <image src="{{avatarUrl}}"></image>
      <view> {{nickName}}</view>
    </view>
    <!--头像&昵称-->
    </view>

  </view>


  <view wx:if="{{avatarUrl==''}}" class="else">
    您尚未登录！
  </view>

